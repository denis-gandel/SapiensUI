import { Primary, Secondary, Link } from "./button.stories.tsx";

<Meta title="Button" />

# Button

Componente de botón con diferentes especializaciones.

#### Primary Example

<Canvas of={Primary} />

#### Secondary Example

<Canvas of={Secondary} />

#### Link Example

<Canvas of={Link} />

## Usage

```ts
import { Button } from "sapiensui";

const Example = () => {
  return (
    <Button
        text="Button"
        disabled=false
        onClick=()=>{}
        variant="primary"
      />
  );
};

export default Example;
```
